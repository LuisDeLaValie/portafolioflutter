import 'package:flutter/material.dart';

class ResumenView extends StatelessWidget {
  const ResumenView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
return Container(
      height:700,
      color: Colors.brown,
      child: Text('Resumen'),
    );  }
}